global:
  configMapName: dev-configmap
  activeProfile: dev
  mongoUrl: mongodb://demo:demo@dev-env-mongodb:27017/?authSource=admin

mongodb:
  auth:
    enabled: true
    databases:
      - catalog
    usernames:
      - demo
    passwords:
      - demo

