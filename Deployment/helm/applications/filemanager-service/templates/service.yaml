apiVersion: v1
kind: Service
metadata:
  name: {{ include "filemanager-service.fullname" . }}
  labels:
    {{- include "filemanager-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "filemanager-service.selectorLabels" . | nindent 4 }}
