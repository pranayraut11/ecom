apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "filemanager-service.fullname" . }}-test-connection"
  labels:
    {{- include "filemanager-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "filemanager-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
