deploymentName : catalog
deploymentLabel : catalog-deployment

replicaCount: 1

image:
  repository: pranayraut11/catalog-app
  tag: latest

containerPort: 8080

service:
  type: ClusterIP
  port: 7001
  targetPort: 8080

config:
  enabled: true
profile:
  enabled: true
mongo:
    enabled: true
redis: 
  enabled: false    

