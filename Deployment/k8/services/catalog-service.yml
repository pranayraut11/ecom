apiVersion: v1
kind: Service
metadata:
  name: catalog
  labels:
    name: catalog
    app: ecom

spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30007
  selector:
    name: catalog-pod
    app: ecom    
